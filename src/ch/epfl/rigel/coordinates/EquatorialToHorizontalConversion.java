package ch.epfl.rigel.coordinates;

import java.time.*; //TODO ask if import * or just what we need

// représente un changement de systèmes de coordonnées
// depuis les coordonnées équatoriales vers les coordonnées écliptiques, à un instant et pour un lieu donnés.
public final class EquatorialToHorizontalConversion {
    public EquatorialToHorizontalConversion(ZonedDateTime when, GeographicCoordinates where){

    }
}
